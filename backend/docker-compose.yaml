services:
  postgres:
    image: postgres:18-alpine
    container_name: db_ecommerce
    restart: unless-stopped

    env_file:
      - .env

    ports:
      - "5433:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/ecommerce

volumes:
  postgres_data:
