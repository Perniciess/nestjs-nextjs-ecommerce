apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: pg-cluster
  namespace: database
spec:
  instances: 2

  storage:
    size: 10Gi
    storageClass: local-path

  affinity:
    nodeSelector:
      workload: db
