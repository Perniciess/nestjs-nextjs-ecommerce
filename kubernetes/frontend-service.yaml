apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: e-commerce
spec:
  selector:
    app: frontend
  ports:
    - port: 3001
      targetPort: 3001
      nodePort: 30001
  type: ClusterIP


